#!/bin/bash

echo "🚀 بدء عملية النشر..."

# التحقق من وجود تغييرات
if [[ -z $(git status -s) ]]; then
  echo "✅ لا توجد تغييرات للنشر"
  exit 0
fi

# إضافة جميع الملفات
echo "📦 إضافة الملفات..."
git add .

# طلب رسالة الـ commit
echo "📝 أدخل رسالة التحديث:"
read commit_message

# إذا لم يتم إدخال رسالة، استخدم رسالة افتراضية
if [ -z "$commit_message" ]; then
  commit_message="تحديث: $(date '+%Y-%m-%d %H:%M:%S')"
fi

# عمل commit
echo "💾 حفظ التغييرات..."
git commit -m "$commit_message"

# رفع إلى GitHub
echo "⬆️  رفع إلى GitHub..."
git push origin main

echo "✅ تم النشر بنجاح!"

# إذا كنت تستخدم GitHub Pages، سيتم النشر تلقائياً
echo "🌐 جاري نشر الموقع على GitHub Pages..."
echo "🎉 الموقع سيكون متاحاً على: https://yourusername.github.io/words-crash-game/"